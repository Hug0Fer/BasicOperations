Algoritmo OperacoesBasicas
Variáveis n1, n2: inteiro;
Início
       Escrever("Introduza um nº inteiro:");
       Ler(n1);
       Escrever("Introduza outro nº inteiro:");
       Ler(n2);

       soma <- a + b;
       subtracao <- a - b;
       multiplicacao <- a * b;
       divisao <- a /b;
       
       Escrever("Soma:", soma);
       Escrever("Subtração:", subtracao);
       Escrever("Multiplicação:", Multiplicacao);
       Escrever("Divisão:", divisao);
Fim.